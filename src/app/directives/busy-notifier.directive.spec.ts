import { BusyNotifierDirective } from "./busy-notifier.directive";


describe('BusyNotifierDirective', () => {
  it('should create an instance', () => {
    const directive = new BusyNotifierDirective();
    expect(directive).toBeTruthy();
  });
});
