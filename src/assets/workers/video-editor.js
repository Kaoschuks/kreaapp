
self.addEventListener('message', event => {
    const actions = event.data
})

function editing_functions(action) {
    switch (action) {
        default:
            break;
    }
}

function apply_filter(data, effect) {
    return data;
}

function apply_adjust(data) {
    return data;
}

function apply_effect(data) {
    return data;
}